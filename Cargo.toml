[package]
name = "soyabean"
version = "0.1.0"
edition = "2021"
description = "A minimal yet powerful terminal code IDE"

[dependencies]
crossterm = { version = "0.28", features = ["bracketed-paste"] }
unicode-width = "0.2"

[profile.release]
lto = true
strip = true
opt-level = 3
