//! soyabean core: text buffer, syntax highlighting, editor state machine and
//! file finder. Shared by the terminal (TUI) and the native GUI binaries.

pub mod buffer;
pub mod draw;
pub mod editor;
pub mod finder;
pub mod syntax;
